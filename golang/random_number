// Online Go compiler to run Golang program online
// Print "Try programiz.pro" message

package main

import (
	"fmt"
	"math/rand"
	"time"
)

func generateRandom(min int, max int) {
	rand.Seed(time.Now().UnixNano())
	random := rand.Intn(max-min+1) + min
	fmt.Println(random)
}

func main() {
	a := [][]int{
		{5, 10},
	}
	for i := 0; i < 100; i++ {
		generateRandom(a[0][0], a[0][1])
	}
}
